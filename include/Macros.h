#pragma once

enum class gameObjectId { boat, moves, reset, tile, frog,nextLevel ,x,undo};
const int SPACE = 80, MARGIN_RIGHT = 350, MARGIN_TOP = 100;
enum class colorId { white, black, gray };
enum class menu { moves,undo,restart,close=3,nextLevel=4};
